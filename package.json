{
  "name": "luci-plugin-2fa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LuCI Two-Factor Authentication Plugin",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:totp": "node tests/totp-verification.test.js",
    "lint": "echo 'No linting configured'",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "otpauth": "^9.2.0",
    "playwright": "^1.40.0",
    "prettier": "^3.1.0",
    "typescript": "^5.9.3",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
